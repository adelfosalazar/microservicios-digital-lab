server:
#  port: 8761
  port: 8099
  
eureka:
  instance:
    hostname: http://${server.address}:${server.port}
    preferIpAddress: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
#    server:
#      waitTimeInMsWhenSyncEmpty: 0
#  dashboard:
#    path: /dashboard
#  instance:
#    hostname: localhost
#    http://localhost:8099/dashboard